apiVersion: apps/v1
kind: Deployment
metadata:
  name: sonarr
  namespace: home-samba
spec:
  replicas: 1
  strategy: {}
  selector:
    matchLabels:
      app: sonarr
  template:
    metadata:
      labels:
        app: sonarr
    spec:
      containers:
      - name: sonarr
        image: ghcr.io/linuxserver/sonarr:develop@sha256:df411a544d547566940b1ddc56d01529d219653af4a3b2e35f5b59de66a01d5b
        imagePullPolicy: Always
        resources: {}
        env:
        - name: PUID
          value: "1000"
        - name: PGID
          value: "1000"
        - name: TZ
          value: "Europe/Dublin"
        - name: POSTGRES_HOST
          value: "postgres-service"
        volumeMounts:
        - name: samba-config
          mountPath: /config
        - name: sonarr-service
          mountPath: /downloads
      volumes:
      - name: samba-config
        persistentVolumeClaim:
          claimName: sonarr-config
      - name: sonarr-service
        persistentVolumeClaim:
          claimName: samba-pvc