---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Phase 1: the direct nodes carry the group's provenance label so the
  # controller can adopt them without interrupting live volumes.
  - ./nodes.yaml
  - ./nodegroup.yaml
  - ./storageclasses.yaml
