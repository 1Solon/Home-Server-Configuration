apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: config-syncer
  namespace: kubeops
spec:
  interval: 1m
  releaseName: config-syncer
  chart:
    spec:
      chart: config-syncer
      version: "0.15.0"
      sourceRef:
        kind: HelmRepository
        name: config-syncer