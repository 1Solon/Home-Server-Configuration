---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: reloader
  namespace: kube-system
spec:
  interval: 30m
  chart:
    spec:
      chart: reloader
      version: 1.0.116
      sourceRef:
        kind: HelmRepository
        name: stakater-charts
        namespace: kube-system
      interval: 5m