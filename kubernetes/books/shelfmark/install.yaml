---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: shelfmark
  namespace: flux-system
spec:
  components:
    - ../../../components/kopiur
  targetNamespace: books
  commonMetadata:
    labels:
      app.kubernetes.io/name: shelfmark
  dependsOn:
    - name: kopiur-repository-books
      namespace: flux-system
  path: "./kubernetes/books/shelfmark/app"
  prune: false
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  interval: 30m
  retryInterval: 1m
  timeout: 5m
  postBuild:
    substitute:
      APP: shelfmark-config
      KOPIUR_SOURCE_PVC: shelfmark-config-kopiur-replacement
      KOPIUR_SCHEDULE: "33 */6 * * *"
      KOPIUR_SUSPEND: "false"
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings
        optional: false
