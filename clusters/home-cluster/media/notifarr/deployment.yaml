apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifiarr
  namespace: home-samba
spec:
  replicas: 1
  strategy: 
    type: Recreate
  selector:
    matchLabels:
      app: notifiarr
  template:
    metadata:
      labels:
        app: notifiarr
    spec:
      containers:
        - name: notifiarr
          image: golift/notifiarr:latest@sha256:5978e9c0dbdacfc5ba6db840aac7a21f6ab7a325122970dd19b294ec91032ffa
          imagePullPolicy: Always
          resources: {}
          ports:
            - containerPort: 5454
          volumeMounts:
            - name: appdata
              mountPath: /config
            - name: appdata
              mountPath: /var/run/utmp
            - name: appdata
              mountPath: /etc/machine-id
      volumes:
        - name: appdata
          persistentVolumeClaim:
            claimName: notifiarr-pvc
