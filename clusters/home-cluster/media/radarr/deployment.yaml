apiVersion: apps/v1
kind: Deployment
metadata:
  name: radarr
  namespace: home-samba
spec:
  replicas: 1
  selector:
    matchLabels:
      app: radarr
  template:
    metadata:
      labels:
        app: radarr
    spec:
      containers:
      - name: radarr
        image: ghcr.io/linuxserver/radarr:develop@sha256:fade39e5149cbebdf04360984781afc9166f5f445f69bbb29a8bbf24bc1dd817
        imagePullPolicy: Always
        resources: {}
        env:
        - name: PUID
          value: "1000"
        - name: PGID
          value: "1000"
        - name: TZ
          value: "Europe/Dublin"
        - name: POSTGRES_HOST
          value: "postgres-service"
        volumeMounts:
        - name: samba-config
          mountPath: /config
        - name: radarr-downloads
          mountPath: /downloads
      volumes:
      - name: samba-config
        persistentVolumeClaim:
          claimName: radarr-config
      - name: radarr-downloads
        persistentVolumeClaim:
          claimName: samba-pvc