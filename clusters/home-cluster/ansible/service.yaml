apiVersion: v1
kind: Service
metadata:
  name: ansible-service
  namespace: ansible 
spec:
  type: LoadBalancer
  selector:
    app: ansible-semaphore 
  ports:
    - protocol: TCP
      port: 3000 
      targetPort: 80 
