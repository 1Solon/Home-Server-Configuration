---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app hermes
  namespace: flux-system
spec:
  components:
    - ../../../components/kopiur
  dependsOn:
    - name: firecrawl
    - name: grafana-mcp
    - name: kopiur-repository-ai
      namespace: flux-system
  path: "./kubernetes/ai/hermes/app"
  targetNamespace: ai
  prune: true
  wait: true
  interval: 30m
  retryInterval: 1m
  timeout: 10m
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  sourceRef:
    kind: GitRepository
    name: flux-system
  postBuild:
    substitute:
      APP: hermes
      KOPIUR_SOURCE_PVC: hermes-kopiur-replacement
      KOPIUR_SCHEDULE: "9 */6 * * *"
      KOPIUR_SUSPEND: "false"
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings
        optional: false
