version: "3"

tasks:
  kopiur:
    desc: Run the repository-pinned Kopiur CLI
    cmds:
      - ./scripts/kopiur {{.CLI_ARGS}}
    silent: true

  gen:
    desc: Generate cluster configurations
    dir: talos
    cmds:
      - talhelper genconfig
    silent: false
