---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: libation-config-miroir
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: miroir-replicated
  resources:
    requests:
      storage: 5Gi
  dataSourceRef:
    apiGroup: volsync.backube
    kind: ReplicationDestination
    name: libation-config-miroir-restore
