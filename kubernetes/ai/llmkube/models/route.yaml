---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: glm-4-7-flash
spec:
  parentRefs:
    - name: envoy-internal
      namespace: networking
      sectionName: https
  hostnames:
    - "glm-flash.${LOCAL_DOMAIN}"
  rules:
    - backendRefs:
        - name: glm-4-7-flash
          port: 8080
