---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app zerobyte
  namespace: flux-system
spec:
  targetNamespace: zerobyte
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  path: "./kubernetes/misc/zerobyte/zerobyte/app"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: false
  interval: 30m
  retryInterval: 1m
  timeout: 5m
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings
        optional: false
