---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app bookorbit
spec:
  interval: 30m
  timeout: 15m
  chart:
    spec:
      chart: app-template
      version: 5.1.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    defaultPodOptions:
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault

    controllers:
      bookorbit:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18@sha256:b6d3af974df781c673d37e49bdddfa14a6f5be28b18d2cb7713f0449bea4057b
            envFrom: &envFrom
              - secretRef:
                  name: bookorbit-secret
            securityContext: &securityContext
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
          init-extensions:
            image:
              repository: docker.io/library/postgres
              tag: 18.6-alpine@sha256:d3e1620b530c944afa6e887d22eb899824da68e19c52024bf98f5220c88a65b2
            command:
              - /bin/sh
              - -ec
              - |
                export PGPASSWORD="$INIT_POSTGRES_SUPER_PASS"
                psql \
                  --host="$INIT_POSTGRES_HOST" \
                  --username="$INIT_POSTGRES_SUPER_USER" \
                  --dbname="$INIT_POSTGRES_DBNAME" \
                  --set=ON_ERROR_STOP=1 <<'SQL'
                CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
                CREATE EXTENSION IF NOT EXISTS pg_trgm;
                CREATE EXTENSION IF NOT EXISTS unaccent;
                CREATE EXTENSION IF NOT EXISTS vector;
                SQL
            env:
              HOME: /tmp
            envFrom: *envFrom
            securityContext: *securityContext
        containers:
          app:
            image:
              repository: ghcr.io/bookorbit/bookorbit
              tag: 2.8.0@sha256:545a874debdb84a04c51b6bb2204376098779a227533bac69f5b087c034ffcff
            env:
              TZ: ${TZ}
              NODE_ENV: production
              HOME: /tmp
              PORT: &port 3000
              APP_URL: https://bookorbit.${LOCAL_DOMAIN}
              CLIENT_URL: https://bookorbit.${LOCAL_DOMAIN}
              OIDC_ALLOW_LOCAL_ISSUERS: "true"
              POSTGRES_HOST: pgvector17-rw.databases.svc.cluster.local
              POSTGRES_PORT: "5432"
              POSTGRES_USER: bookorbit
              POSTGRES_DB: bookorbit
              PUID: "1000"
              PGID: "1000"
              BOOKORBIT_FIX_PERMISSIONS: "false"
              NODE_MAX_OLD_SPACE_SIZE: auto
              LIBRARY_BROWSE_ROOT: /libraries
              BOOK_DOCK_PATH: /data/book-dock
              POSTGRES_PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: bookorbit-secret
                    key: POSTGRES_PASSWORD
              JWT_SECRET:
                valueFrom:
                  secretKeyRef:
                    name: bookorbit-secret
                    key: JWT_SECRET
              SETUP_BOOTSTRAP_TOKEN:
                valueFrom:
                  secretKeyRef:
                    name: bookorbit-secret
                    key: SETUP_BOOTSTRAP_TOKEN
            probes:
              liveness: &probe
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/v1/health
                    port: *port
                  initialDelaySeconds: 10
                  periodSeconds: 30
                  timeoutSeconds: 5
                  failureThreshold: 3
              readiness: *probe
              startup:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/v1/health
                    port: *port
                  periodSeconds: 5
                  timeoutSeconds: 5
                  failureThreshold: 60
            securityContext: *securityContext
            resources:
              requests:
                cpu: 100m
                memory: 1Gi
              limits:
                memory: 2Gi

    service:
      app:
        controller: *app
        ports:
          http:
            port: *port

    route:
      app:
        enabled: true
        parentRefs:
          - name: envoy-internal
            namespace: networking
            sectionName: https
        hostnames:
          - "bookorbit.${LOCAL_DOMAIN}"
        rules:
          - backendRefs:
              - name: *app
                port: *port

    persistence:
      data:
        existingClaim: bookorbit-kopiur-replacement
        globalMounts:
          - path: /data
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
      book-dock:
        type: nfs
        server: ${TRUENAS_IP}
        path: /mnt/STORAGE-01/Media-Storage/downloads/shelfmark-ingest/books
        globalMounts:
          - path: /data/book-dock
      books:
        type: nfs
        server: ${TRUENAS_IP}
        path: /mnt/STORAGE-01/Media-Storage/books
        globalMounts:
          - path: /libraries/books
      manga:
        type: nfs
        server: ${TRUENAS_IP}
        path: /mnt/STORAGE-01/Media-Storage/manga
        globalMounts:
          - path: /libraries/manga
