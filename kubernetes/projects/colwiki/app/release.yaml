---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app otterwiki
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 4.5.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      otterwiki:
        annotations:
          reloader.stakater.com/auto: "true"

        initContainers:
          init-permissions:
            image:
              repository: redimp/otterwiki
              tag: 2.15.1-slim@sha256:cc818c0d8acde43776fa4d58ac3513110be7846412d4aebe7929c7bf3b6e729f
            command:
              - sh
              - -c
              - |
                chown -R 33:33 /app-data
                chmod -R 755 /app-data
            securityContext:
              runAsUser: 0
              runAsGroup: 0

        containers:
          app:
            image:
              repository: redimp/otterwiki
              tag: 2.15.1-slim@sha256:cc818c0d8acde43776fa4d58ac3513110be7846412d4aebe7929c7bf3b6e729f
            env:
              TZ: "${TZ}"
              PUID: 33
              PGID: 33
              SITE_NAME: "Collective-Wiki"
              SITE_LOGO: "/Home/a/col_logo_cyan_glow5.webp"
              SITE_ICON: "/Home/a/col_logo_cyan_glow5.webp"
              SITE_DESCRIPTION: "Wiki for Collective multi-game community"
              READ_ACCESS: "APPROVED"
              WRITE_ACCESS: "APPROVED"
              ATTACHMENT_ACCESS: "APPROVED"
              DISABLE_REGISTRATION: "false"
              AUTO_APPROVAL: "false"
              SIDEBAR_MENUTREE_MODE: "SORTED"
              GIT_WEB_SERVER: "true"
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              runAsNonRoot: true
              runAsUser: 33
              runAsGroup: 33
              capabilities:
                drop:
                  - ALL
            resources:
              requests:
                cpu: 10m
                memory: 100Mi
              limits:
                memory: 500Mi

    service:
      app:
        controller: *app
        ports:
          http:
            port: &port 8080

    probes:
      liveness:
        enabled: true
        custom: true
        spec:
          httpGet:
            path: /
            port: *port
          initialDelaySeconds: 30
          periodSeconds: 60
          timeoutSeconds: 10
          failureThreshold: 3

      readiness:
        enabled: true
        custom: true
        spec:
          httpGet:
            path: /
            port: *port
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 3

    route:
      app:
        enabled: true
        annotations:
          external-dns.alpha.kubernetes.io/target: ipv4.${REMOTE_DOMAIN}
          gethomepage.dev/enabled: "true"
          gethomepage.dev/name: OtterWiki
          gethomepage.dev/description: Wiki for documentation
          gethomepage.dev/group: Applications
          gethomepage.dev/icon: otter-wiki-dark.svg
        parentRefs:
          - name: envoy-external
            namespace: networking
            sectionName: https
        hostnames:
          - "col-wiki.${REMOTE_DOMAIN}"
        rules:
          - backendRefs:
              - port: *port
                name: *app

    persistence:
      app-data:
        existingClaim: otterwiki
        globalMounts:
          - path: /app-data

      app:
        type: emptyDir
        globalMounts:
          - path: /app

      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
